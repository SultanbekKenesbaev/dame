# Windows pilot checklist

## Два режима Windows

- **Полный kiosk:** Windows Enterprise, Education или IoT Enterprise. Shell Launcher заменяет Explorer, действует принудительный выход в `04:00`, а стандартный пользователь не может закрыть тест обычными средствами.
- **Демонстрационный режим:** Windows 10/11 Pro. Клиент автоматически открывается поверх рабочего стола при входе, закрывает все мониторы и не показывает обычную кнопку выхода во время исправного теста. Этот режим можно обойти через Task Manager, перезагрузку и другие системные средства, поэтому он предназначен для проверки сценария, API и интерфейса, а не для принудительного допуска сотрудников.

В деморежиме принудительный выход из Windows в `04:00` отключён. Клиент сам покажет тест нового рабочего дня. Если служба недоступна или отправка технически завершилась ошибкой, появляется кнопка безопасного возврата на рабочий стол.

## Перед установкой

- Проверить редакцию Windows и выбрать цель пилота: полный kiosk на Enterprise/Education/IoT либо демонстрацию на Pro.
- Создать и протестировать отдельного локального Windows-администратора, не затрагиваемого kiosk-политиками.
- Создать полную резервную копию или recovery image компьютера.
- Перенести нужные рабочие файлы и настройки в будущий kiosk-профиль.
- Проверить доступность API по HTTPS и корректность системного времени.
- Получить code-signing сертификат и собирать через `build-windows.ps1 -CertificateThumbprint ...`; скрипт подписывает и проверяет службу, клиент и MSI. Неподписанный prototype нельзя массово разворачивать.

## Установка

1. Установить `DailyGate.Setup.msi` из elevated console.
2. Создать сотрудника и enrollment code в админке.
3. Открыть PowerShell от имени администратора и запустить `Provision-Device.ps1` с URL, кодом и понятным именем ПК:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File "$env:ProgramFiles\DailyGate\Tools\Provision-Device.ps1" `
  -ApiUrl "https://dailygate.example.com" `
  -EnrollmentCode "КОД-ИЗ-АДМИНКИ" `
  -DeviceName $env:COMPUTERNAME
```

Параметр изменяет execution policy только для запущенного процесса и не меняет постоянную политику Windows.
4. Перезагрузить Windows. В деморежиме достаточно выйти и снова войти в текущую учётную запись.
5. В полном kiosk убедиться, что auto-logon открывает DailyGate, а Explorer отсутствует до отправки. На Pro проверить автозапуск клиента с отметкой `ДЕМО-РЕЖИМ · WINDOWS PRO`.
6. Войти временным паролем, сменить его, пройти тест и проверить запуск Explorer.
7. Перезагрузить ПК в те же рабочие сутки: повторный тест не должен появиться.

## Обязательные испытания

- Отключить интернет и пройти заранее загруженный тест; после сети проверить синхронизацию.
- Остановить API не дольше семи дней и проверить offline lease.
- Проверить expiry таймера, частичные ответы и статус `TimedOut`.
- В полном kiosk закрыть клиент через Alt+F4 и Task Manager под kiosk-профилем; доступ не должен появиться.
- На Windows Pro проверить, что при исправной службе обычной кнопки выхода нет, а при остановленной службе появляется безопасный выход. Отдельно зафиксировать, что Task Manager остаётся доступным как ограничение деморежима.
- Проверить второй монитор: он должен оставаться закрытым shield-окном.
- Сгенерировать emergency code, применить один раз и проверить аудит/replay rejection.
- Сдвинуть часы назад в тестовой среде и проверить security event.
- В тестовый день временно изменить границу рабочего дня на ближайшее время и проверить предупреждения/logoff.
- Зайти отдельным локальным администратором и выполнить recovery.

## Аварийное снятие kiosk или деморежима

1. Войти отдельным локальным Windows-администратором.
2. Запустить elevated PowerShell:

```powershell
& "$env:ProgramFiles\DailyGate\Tools\Remove-Kiosk.ps1"
```

3. Перезагрузить Windows.
4. После появления обычного Explorer удалить DailyGate через Installed Apps. На Windows Pro скрипт удаляет задачу автозапуска демо-клиента.

Если Windows не позволяет войти локальным администратором, использовать Windows Recovery Environment и заранее подготовленную recovery-процедуру организации.
