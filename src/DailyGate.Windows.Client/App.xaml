<Application x:Class="DailyGate.Windows.Client.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Startup="Application_Startup"
             Exit="Application_Exit"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <SolidColorBrush x:Key="Ink" Color="#10201D" />
        <SolidColorBrush x:Key="Muted" Color="#6D7C78" />
        <SolidColorBrush x:Key="Green" Color="#24B485" />
        <SolidColorBrush x:Key="Navy" Color="#0E211F" />
        <Style TargetType="Button">
            <Setter Property="Padding" Value="18,12" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Background" Value="{StaticResource Green}" />
            <Setter Property="Foreground" Value="#071913" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="13" />
            <Setter Property="BorderBrush" Value="#CFDAD6" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="15" />
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="13" />
            <Setter Property="BorderBrush" Value="#CFDAD6" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="15" />
        </Style>
    </Application.Resources>
</Application>
